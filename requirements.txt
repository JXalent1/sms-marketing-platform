# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.3

# Config & validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1
# psycopg2-binary==2.9.9        # uncomment for PostgreSQL

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                   # MUST stay 4.0.x — 4.1+ breaks passlib 1.7.4
itsdangerous==2.2.0

# Rate limiting
slowapi==0.1.9

# Scheduling
apscheduler==3.10.4
pytz==2024.1

# HTTP
httpx==0.26.0

# SMS providers — install only what you use
#
# telnyx MUST stay on 4.x. app/sms/providers/telnyx.py is written against the
# 4.x client object (`telnyx.Telnyx(api_key=...)`, `client.messages.send()`);
# 2.x has no such class, so TelnyxProvider.__init__ raised AttributeError,
# get_provider() caught it and fell back to console, and the dashboard went on
# showing a normal "Dry run" pill on a box that had been switched live. The pin
# said 2.1.2 for the whole of module 5b and nothing failed loudly.
telnyx==4.175.0
twilio==9.0.4
